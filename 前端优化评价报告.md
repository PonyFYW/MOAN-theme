# 墨案缉凶 · 主题网页版 — 前端设计评价报告

> 目标：设计一个「有风格的、去AI味的、玩家交互体验好的」小游戏前端网页（对标 Murdoku）。
> 范围：`theme-web/`（线上 `https://moan-theme.ryanwuu.workers.dev/`）与渲染层源码 `minigame/src/ui/`。

---

## 一、结论先行（TL;DR）

**这个项目最值钱的东西其实已经写好了，只是还没被搬进正式游戏。** `theme-web/_mock-gongtang.html` 是一份完成度很高的「水墨公堂」静态视觉稿——宣纸纤维纹、淡墨山水、朱砂印章糙边、绫绢装裱金线、毛笔/砚台/卷轴手绘工具图标、Murdoku 式三列嫌疑人卡。它定义了「墨案」应有的样子。但真正渲染给玩家看的 `home.js / cases.js / play.js`，仍然是一套更扁平、更「功能型 H5」的画风：纯色填充、emoji 工具图标、亮色马卡龙地板、系统默认字体。

所以**最大的优化空间不在「再想一个风格」，而在「把 mock 里已经想好的风格，真正搬进游戏渲染层」**。其次才是两块硬骨头：**去 AI 味（美术资产）** 与 **动效/反馈**。

按优先级，一句话排序：

1. **P0 视觉一致性**：把宣纸/墨色/印章/装裱纹理从 mock 移植进正式渲染，统一色板与字体。
2. **P0 去 AI 味**：22+ 头像、40+ 物件、地板全部是 Meshy AI 生成的写实渲染图，与「手绘案卷」气质根本冲突，是 AI 味最集中处。
3. **P1 交互反馈**：补落子/排除/呈堂/胜利的微动画与音效震动，让操作「有手感」。
4. **P1 桌面三栏布局**：横屏大屏真正用上 mock 的「左证词册页 / 中棋盘 / 右工具」三栏，而非现在只是把竖屏单列转一下。
5. **P2 新手引导、错误定位、键盘快捷键、加载体积**。

---

## 二、现状盘点（已经做对的地方，别动）

- **墨底 + 朱砂 + 金 的三色骨架是对的**：`#121510` 墨底 / `#b84038` 朱砂 / `#c9a35c` 金，已经有「南宋公案 + 水墨」的调性，不是随手配的。
- **印章「墨案」是识别度最高的一块品牌资产**，保留并强化。
- **天干地支坐标**（甲乙丙丁…丑）是绝佳的文化钩子，比 1–12 数字强太多。
- **交互手势已有不错底子**：单击批注 / 长按放置 / 滑动连续批注 / 右键排除 / 撤回 / 提点 / 自动排除 / 呈堂，PC 悬浮高亮、滚轮滚动都接好了。
- **工程分层干净**：场景管理器（RAF + 脏标记）、离屏静态缓存、`wx`→Web 兼容层、内联资产，都到位，可维护性好。

这些是地基，优化别推翻它们。

---

## 三、优化空间（按目标维度）

### 1. 有风格 — 视觉与品牌一致性

| 现状问题 | 优化建议 |
|---|---|
| `_mock-gongtang.html` 的宣纸纹理/淡墨山水/四角暗角/绫绢金线是「设计目标」，但 `play.js` 主渲染仍是纯色填充，两者脱节 | 把 mock 里的 `paperGrain()`（宣纸纤维 + 云絮）、`inkMountains()`（淡墨山水）、四角暗角、棋盘「绫绢装裱 + 描金线」直接移植进正式渲染层，做成可复用的装饰函数 |
| 字体全靠系统默认（`sans-serif` / `serif` / `monospace` 混用），iOS/Android/Windows 缺省字体差异极大，楷体宋体可能根本不生效 | 锁定三级字阶并内嵌或声明 WebFont：**案名=楷体大字、正文=宋体、计时数字=等宽**；至少用 `Noto Serif SC` + `KaiTi/STKaiti` 回退链（mock 里已经写了正确的字体栈，照搬） |
| 地板色 `FLOOR_COLORS`（`data.js`）是亮色马卡龙（`#93c96b` 草绿、`#f3dde3` 粉），与墨色暗调冲突，是「数独 H5」残留 | 地板改走 mock 的**纹理贴图**或低饱和墨色/宣纸色阶，弃用高饱和马卡龙 |
| 工具图标用 emoji（✕ ⌫ ↩ 💡 ⚡）和文字，通用 H5 味重 | 换成 mock 里已画好的**文房四宝手绘图标**（毛笔=批注、朱砂叉=排除、砚台=擦除、卷轴=提点），统一描边线稿风格 |

### 2. 去 AI 味 — 美术资产（最硬的一块）

**现状**：`js/assets-inline.js` 内联 1.4MB base64，全部来自 Meshy AI：

- 身份头像 ~22 套（`Meshy_AI_character-*.png` + `cut-*` 透明底），写实 3D 渲染风。
- 物件 ~40 个（`Meshy_AI_prop-*.png`），写实渲染风。
- 地板 12 色 + 3 张 AI 贴图（`Meshy_AI_tile-*.png`）。

AI 生成的写实人物头像，与「水墨公堂手绘案卷」在美学上**根本冲突**：过度光滑的皮肤、塑料/3D 反光、统一比例的「游戏角色脸」。这是整个项目「AI 味」最集中的地方。

**去 AI 味方案（按成本从低到高，可叠加）**：

1. **统一后期滤镜压塑感（最快见效）**：给所有头像/物件在 canvas 绘制时套一层统一滤镜——降饱和 + 提对比 + 墨线描边（`ctx.filter = 'saturate(0.5) contrast(1.1)'` 或描边），把写实渲染往「工笔 / 木刻 / 版画」靠。零重画成本。
2. **头像改「朱砂单字印章 + 白描线稿」**：不再放写实人脸，改用每人一个朱砂印章单字（管/春/熙…）+ 极简白描剪影。风格彻底统一，且和棋盘上的 token 简称徽章天然一致（mock 的 `TOKENS` 已经在用「管/春/熙」单字徽章，方向一致）。
3. **重画/换线稿资产**：人物换白描线稿或传统工笔，物件换描边单色图标（几十 KB 矢量即可）。
4. **至少改掉 `Meshy_AI_` 文件名与体积**：1.4MB 内联 → 按主题懒加载 + 雪碧图 + 线稿化后压到几百 KB 内；`Meshy_AI_` 前缀本身也是「AI 味」的明示，应重命名。

> 关键判断：**「去 AI 味」不等于「换更好的 AI 图」**。只要继续用写实渲染图，AI 味就去不掉。正确方向是「降维」——从写实渲染降到线稿/印章/剪影，让风格本身手绘化，而不是让 AI 画得更像人。

### 3. 玩家交互体验

已具备：单击批注 / 长按放置 / 滑动连续批注 / 右键排除 / 撤回 / 提点 / 自动排除 / 呈堂 / 悬浮高亮 / toast。

**缺口与建议**：

- **无新手引导（onboarding）**：现在只有文字「玩法说明」弹窗，首局玩家要自己摸索「单击 vs 长按」双轨交互。建议首局做 3 步交互式引导：引导你点一次批注 → 长按放一次 → 打一次叉，边做边提示。
- **反馈动画几乎为零**：放置/排除/撤回都是离屏缓存静态重绘，无过渡。Murdoku 的爽感来自微动画。建议：落子弹性缩放、错误格抖动、撤回淡出、胜利「朱砂大印盖下 + 红泥拓印」动画（mock 里 `seal()` 的糙边印章就是现成素材）。
- **呈堂错误反馈弱**：当前只提示「几处与现场不符」，没指出哪几处。建议高亮错误格 + 错误计数，且给一次「继续改」不立刻判负的缓冲。
- **触感缺失**：有 `vibrateShort`（12ms），但没在关键节点调用。放置/呈堂成功/错误各配一档震动 + 短音效。
- **键盘与无障碍（PC）**：无快捷键。建议数字键选人、方向键移动光标、`Z` 撤回、`Esc` 关弹窗；悬浮提示扩展为「悬停某格显示可放置的证词约束」。
- **进度呈现**：卷宗列表用锁印表示完成，建议加完成度百分比、最优用时、连破纪录，增强复玩动力。

### 4. 布局与响应式

- 已有横竖屏自适应 + 安全区处理，手机竖屏单列布局是合理的。
- **桌面大屏没真正利用宽度**：横屏当前只是「把单列转一下」。mock 是 1440×900 三栏（左证词册页 / 中棋盘 / 右文房工具），这正是桌面该有的样子。建议加一个 desktop 断点（如 `min-width: 900px`）：三栏铺开，证词卡改 Murdoku 式三列网格，工具栏竖排到右侧；手机竖屏维持现单列。同一套渲染代码，两套布局参数。

### 5. 动效与氛围

- 启动页已有脉冲动画（`bootPulse`），是好的开端。
- 游戏内建议补「克制的」氛围动效（都用 `ease-out`，避免花哨）：墨滴晕开、印章盖下、山水淡入、落子墨点、胜利印章拓印。**克制是关键**——公案游戏的调性，动效要「沉、稳、慢」，不是综艺感弹跳。

### 6. 工程与性能

- **1.4MB 内联 base64 是最大体量负担**：首次加载久、内存占用高。建议按主题懒加载 + 雪碧图 + 线稿化压缩（见第 2 节）。
- **中文字体未内嵌**：观感受设备影响大，建议用 `font-display: swap` 的 WebFont 子集，或接受系统字体但锁定回退链。
- 离屏静态缓存 + 脏标记渲染已不错，保留。

---

## 四、优先级落地清单

| 优先级 | 事项 | 成本 | 收益 |
|---|---|---|---|
| **P0** | 把 mock 的宣纸纹理/墨色/装裱金线/印章糙边移植进正式渲染（`play.js`/`home.js`） | 中 | 直接「有风格」，一次到位 |
| **P0** | 统一色板与字体栈（弃马卡龙地板、锁楷体/宋体字阶） | 低 | 风格统一 |
| **P0** | 头像/物件降维：先上统一滤镜压塑感，再推进印章/线稿化 | 低→高 | 去 AI 味 |
| **P1** | 落子/排除/呈堂/胜利的微动画 + 震动音效 | 中 | 交互「有手感」 |
| **P1** | 桌面三栏布局（左证词册页/中棋盘/右工具） | 中 | 桌面体验质变 |
| **P2** | 首局交互式新手引导 | 中 | 降低上手门槛 |
| **P2** | 呈堂错误高亮定位、PC 快捷键、完成度/最优记录 | 低-中 | 体验闭环 |
| **P2** | 资产懒加载 + 体积压缩（1.4MB→<400KB） | 中 | 加载/性能 |

---

## 五、一句话总结

风格方向、品牌资产（印章、天干地支）、交互手势、工程分层**都已经对了**，缺的是「把 mock 画好的水墨公堂真正搬进游戏」这一步，以及「把写实 AI 图降维成手绘线稿/印章」这一步。前者决定「有没有风格」，后者决定「去没去掉 AI 味」。
